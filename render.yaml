services:
  - type: web
    name: alpen-advisor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run alpen_advisor_v46.py --server.port $PORT --server.address 0.0.0.0
    plan: free